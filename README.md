# 📝 Feedback Bot

Telegram-бот для сбора обратной связи и отзывов с модерацией и статистикой.

## 🚀 Возможности

- **Для пользователей:**
  - 📝 Оставить отзыв с текстом и оценкой (1-5 звёзд)
  - 📊 Просмотр своих отзывов
  - ℹ️ Справка по боту

- **Для администраторов:**
  - 📋 Просмотр новых отзывов
  - ✅/❌ Модерация (одобрение/отклонение)
  - 💬 Добавление комментариев
  - 📊 Статистика по отзывам

## 📋 Требования

- Python 3.9+
- MongoDB (локально или Atlas)
- Telegram Bot Token

## ⚙️ Установка

### 1. Клонирование/создание проекта

```bash
cd feedback_bot
```

### 2. Создание виртуального окружения

```bash
python -m venv venv
venv\Scripts\activate  # Windows
source venv/bin/activate  # Linux/Mac
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Настройка окружения

Скопируйте `.env.example` в `.env` и заполните значения:

```bash
cp .env.example .env
```

Откройте `.env` и укажите:

```env
# Токен бота от @BotFather
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

# MongoDB connection string
MONGODB_URI=mongodb://localhost:27017

# ID администратора (ваш Telegram ID)
ADMIN_IDS=123456789

# Название базы данных
DB_NAME=feedback_bot
```

### 5. Настройка MongoDB

**Вариант 1 — Railway (рекомендуется):**

1. Зайди на [railway.app](https://railway.app)
2. New → Database → MongoDB
3. Кликни на сервис → вкладка **Connect**
4. Скопируй **Connection String**
5. Вставь в `.env` в переменную `MONGODB_URI`

**Вариант 2 — Локально:**
```bash
net start MongoDB  # Windows
```

**Вариант 3 — Docker:**
```bash
docker run -d -p 27017:27017 --name mongodb mongo:latest
```

**Вариант 4 — MongoDB Atlas:**
1. [MongoDB Atlas](https://www.mongodb.com/cloud/atlas)
2. Создай бесплатный кластер
3. Получи connection string

### 6. Запуск бота

```bash
python main.py
```

## 🤖 Как получить токен бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Введите имя бота (например, `Feedback Bot`)
4. Введите username бота (например, `my_feedback_bot`)
5. Скопируйте полученный токен

## 📱 Как узнать свой Telegram ID

1. Откройте бота [@userinfobot](https://t.me/userinfobot)
2. Он отправит ваш ID
3. Укажите этот ID в `ADMIN_IDS`

## 🎮 Использование

### Пользователь

1. Запустите бота командой `/start`
2. Нажмите «📝 Оставить отзыв»
3. Напишите текст отзыва
4. Выберите оценку (1-5 звёзд)
5. Готово!

### Администратор

1. Отправьте команду `/admin`
2. Выберите действие:
   - «📋 Новые отзывы» — отзывы на модерации
   - «📊 Статистика» — общая статистика
   - «🔍 Все отзывы» — последние отзывы

## 📁 Структура проекта

```
feedback_bot/
├── bot/                    # Основной модуль бота
├── config/                 # Конфигурация
│   ├── __init__.py
│   └── settings.py         # Настройки из .env
├── database/               # Работа с БД
│   ├── __init__.py
│   ├── connection.py       # Подключение к MongoDB
│   └── models.py           # Модели данных
├── handlers/               # Обработчики
│   ├── __init__.py
│   ├── user.py             # Пользовательские хендлеры
│   └── admin.py            # Админские хендлеры
├── keyboards/              # Клавиатуры
│   ├── __init__.py
│   └── main.py             # Основные клавиатуры
├── middlewares/            # Промежуточное ПО
├── utils/                  # Утилиты
├── .env                    # Переменные окружения
├── .env.example            # Пример .env
├── .gitignore
├── main.py                 # Точка входа
└── requirements.txt        # Зависимости
```

## 🚀 Деплой на Railway

### 1. Подготовка

1. Создай новый проект на [Railway](https://railway.app)
2. Добавь сервис: **Deploy from GitHub repo**
3. Подключи репозиторий с ботом

### 2. Добавь MongoDB

1. В том же проекте: New → Database → MongoDB
2. Дождись развёртывания

### 3. Настрой переменные окружения

В Railway для своего бота добавь переменные:

| Переменная | Значение |
|------------|----------|
| `BOT_TOKEN` | Токен от @BotFather |
| `ADMIN_IDS` | Твой Telegram ID |
| `MONGODB_URI` | Возьми из сервиса MongoDB (вкладка Connect) |
| `DB_NAME` | `feedback_bot` |

### 4. Deploy

Railway автоматически соберёт и запустит бота.

---

## 🔧 Расширение функционала

### Добавление уведомлений пользователю

В файле `handlers/admin.py` в функциях `approve_feedback` и `reject_feedback` можно добавить отправку уведомлений пользователю о статусе отзыва.

### Экспорт отзывов

Можно добавить команду `/export` для экспорта отзывов в CSV/Excel.

### Аналитика

Добавьте графики и аналитику по средним оценкам, динамике отзывов и т.д.

## 📝 Лицензия

MIT

## 👨‍💻 Автор

Создано для демонстрации возможностей Aiogram 3.x + MongoDB
